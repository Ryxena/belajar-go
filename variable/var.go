package main

import "fmt"

func main(){
	var name string = "Rubben"
	var (
		halo = "halo"
		dunia = "world"
	)
	github := "Ryxena"
	fmt.Println(name)
	fmt.Println(halo)
	fmt.Println(dunia)
	fmt.Println(github)
}